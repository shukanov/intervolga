1) ѕри запросе SELECT * FROM a, b WHERE a.id=b.a_id пользователь получает все элементы из таблиц "a" и "b", у которых идентичные пол€ "a.id" и "b.a_id".
2) ѕри запросе SELECT * FROM a JOIN b ON a.id=b.a_id; пользователь также получает только те элементы таблиц "a" и "b", однако, дл€ этого используетс€ оператор соединени€ (внутреннего) JOIN. ¬ этом случае перед выборкой 2 таблицы соедин€ютс€.    